---
name: project_growth_b2b
description: "Post-teaser-epic growth + B2B: social syndication feeds (Idea 1, DONE) + agency team workspaces (Idea 2, RLS foundation DONE, UI pending)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 96606e81-ac29-4269-9a3a-54a9058cf334
---

Two post-[[project_teaser_marketplace]] initiatives. Both are thin adapters on the teaser foundation.

**IDEA 1 — Social syndication (DONE, commit fc224aa, needs web redeploy):** Ports & Adapters — NEXPEC emits ONE sanitized outbound contract; channels live in an external automation layer (Make/Zapier/n8n), ZERO Meta/LinkedIn SDKs/tokens in core. `apps/web/src/app/feed.xml/route.ts` (RSS 2.0, **user-authored**) + `feed.json/route.ts` (JSON Feed 1.1, mine) read `public_demand_feed` (sanitized by construction) → items link to canonical /inspections/<slug>; edge-cached (s-maxage 300). `/discover` advertises the rss alternate (`alternates.types['application/rss+xml']='/feed.xml'`). `app/robots.ts` already → sitemap. **Verified fact (web search 2026-06): Facebook Groups API removed 2024 — NO tool can auto-post to Groups (manual only); FB Pages + LinkedIn Company Pages are tool-driven (w_organization_social, app review); LinkedIn personal limited (w_member_social).** Recommend syndicating a curated/batched DIGEST, not per-row (ToS/spam safety).

**IDEA 2 — Agency Team Workspaces (multi-seat). RECON: org-team subsystem ALREADY EXISTS + is GENERIC to agency+enterprise** — `org_members`(org_id,user_id,role enum org_member_role=owner/procurement_admin/project_lead/viewer), `org_invitations`(token/status/expires), RPCs invite_org_member/accept_org_invitation/revoke_org_invitation/set_active_org, helper `is_member_of_org(p_org_id)`; routes `/orgs/accept/[token]` + `/client/team`; data layers clientTeam.ts/orgMembers.ts/orgStructure.ts/organizations.ts. **jobs has NO org column** — ownership = single user (client_id XOR agency_id). Pre-existing job RLS keyed ONLY to single user ids (jobs_client_self_select = client_id|agency_id = auth.uid(); jobs_update same; + RESTRICTIVE jobs_dept_scoping_restrictive via nx_can_see_job_dept_scoping). Chat = strict silos: `conversations`(user_id ↔ admin via nx_is_admin), messages readable only by conversation.user_id or admin — NEVER client↔inspector; price hidden from inspectors.

**SET 1 DONE (commit f88ee68, PENDING db push 184000):** `20260801184000_agency_team_workspace_rls.sql` — `nx_can_team_access_job(uuid)` (shares an org with the job's owner = COALESCE(agency_id,client_id); any role → VIEW) + `nx_can_team_manage_job(uuid)` (role<>viewer → MANAGE), BOTH SECURITY DEFINER (so the jobs policy doesn't recurse into jobs RLS). PERMISSIVE policies jobs_team_select / jobs_team_update / reports_team_select ADD access (existing owner/admin policies + RESTRICTIVE dept/soft-delete still apply) → **team access ⊆ principal's scope; price-blindness + chat silos untouched.** No jobs schema change / no backfill. Org-generic (agencies + enterprise teams). Self-tests assert helpers SECURITY DEFINER + policies present.

**SET 2 DONE (commits 133a2d8 chat+pgTAP, cdb79ce UI; PENDING db push 186000+188000):**
- Chat RLS (186000): `nx_can_team_access_conversation` / `nx_can_team_manage_conversation` (SECURITY DEFINER) grant teammates ONLY the buyer-side thread (`kind='job_client_admin'` AND `c.user_id = COALESCE(j.agency_id,j.client_id)`) — inspector↔admin thread unreachable (silo preserved by construction). VIEW=any member, POST=non-viewer on open thread. Permissive policies conv_team_select/msg_team_select/msg_team_insert.
- pgTAP `supabase/tests/rls_team_workspace_test.sql` (plan 19, run `supabase test db`): proves mgr sees job/report/buyer-thread + posts; CANNOT see/post inspector thread; viewer can't manage/post; outsider sees nothing; inspector sees only own thread. Harness = seed superuser + `set local role authenticated` + `request.jwt.claims` sub; literal uuids inside `$$` (psql does NOT interpolate :'var' inside dollar-quotes).
- Team Missions UI (188000 + web): `nx_team_jobs()` RPC = price-free org-scoped job projection (raw jobs read would also return open-marketplace rows + price cols). `app/client/team-missions/page.tsx` (RSC) + `lib/data/teamWorkspace.ts` + client Sidebar 'Team Missions' under Jobs. Member management ALREADY exists at `/client/team` (generic agency/enterprise — invite/accept/revoke). Detail/report/chat per-row via team RLS; links to /client/jobs/[id] (exists).

**SET 3 DONE (commits caf1b6d notify, d7ca638 chat UI; PENDING db push 190000):**
- Notify fan-out (190000): tg_notify_messages now TEAM-AWARE for job_client_admin threads whose owner has an org — buyer-side post → notify admins + every other org member; admin reply → every org member; ALL non-team convs (inspector/help_support/solo) unchanged byte-for-byte. Reuses nx_notify(6-arg)/nx_notify_admins(5-arg)/nx_is_admin(uuid) (signatures verified). Trigger binding unchanged.
- In-mission chat UI (d7ca638): `/client/jobs/[id]/chat` reuses MessageThread (realtime) + RichComposer; `fetchTeamChatContext` (teamWorkspace.ts) resolves the job_client_admin conversation + messages + canPost (nx_can_team_manage_conversation rpc) + a user→org-role-label map. MessageThread gained optional `senderRoles` prop → pseudonymous 'Role · NX-handle' attribution for others (ONLY when passed; 1:1 admin threads unchanged; never a real name). Composer hidden for view-only roles. Team Missions rows → Chat link.
- Notes: notify deep-link → /client/jobs/[id] (detail); chat is the /chat subroute (reachable from Team Missions + the notif lands adjacent). Legacy chat_rooms/room_id path NOT extended (conversations/messages = canonical silo model).

**EPIC COMPLETE — Idea 1 (syndication feeds) + Idea 2 (Agency Team Workspaces: RLS foundation 184000 + chat silo 186000 + pgTAP 19 + team views/nx_team_jobs 188000 + member mgmt [pre-existing /client/team] + notify fan-out 190000 + in-mission chat).** All migrations 184000–190000 applied EXCEPT re-pushes noted. Optional future: perf opt (precompute my-org member-id set if jobs grows); chat thread on the mission DETAIL page (vs /chat subroute); enterprise dept-scoped team variants. Recurring Supabase gotcha hit twice: REVOKE FROM PUBLIC,anon (not just PUBLIC) on new public funcs/tables — see [[reference_nexpec_schema_gotchas]].

**DEPLOY for Set 2: `supabase db push` (186000 + 188000) + redeploy web.** Run `supabase test db` to see the 19 pgTAP guards pass.

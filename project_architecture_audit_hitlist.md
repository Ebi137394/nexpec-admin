---
name: project-architecture-audit-hitlist
description: 2026-06-25 holistic audit — P0 mobile-chat RLS break + P1 mobile schema drift; fix in the Mobile Parity Epic. Full report docs/ARCHITECTURE_AUDIT.md
metadata: 
  node_type: memory
  type: project
  originSessionId: 54760b3e-025a-409a-bb7c-3e502cb50675
---

Holistic TS/web/mobile bug-hunt (2026-06-25). Full report: `docs/ARCHITECTURE_AUDIT.md`. Verified against baseline schema, not inferred.

**P0 STATUS — EXECUTED (Mobile Parity Phase 1, code written, PENDING `supabase test db` + push).** Keystone discovery: **`send_message` did NOT exist** as a DB function (useChat called a missing RPC → fell back to a room_id insert the RLS denies). Created it: migration `20260801198000` (SECURITY DEFINER, poster check = admin|owner|non-viewer teammate; triggers `_messages_fill_sender_role`+`_conversation_on_new_message` maintain metadata). Backfill `20260801200000` (DO-block, documented heuristic: inspector-sent→inspector room, rest→client room; idempotent, reversible — rows keep job_id). pgTAP `supabase/tests/rls_messages_silo_test.sql` (plan 9: null-conversation insert DENIED + send_message owner/outsider/admin matrix). Refactored: `src/core/chat/messages.ts` (added getOrCreateJobConversationId/Support, send via send_message, fixed markMessagesRead p_conv_id, removed getChatRoomId), `app/messages/[id].tsx` (inline resolver), `app/chat/[job_id].tsx` (+ killed phantom file_url/file_type), `src/core/chat/hooks/useChat.ts` (send + markAsRead → conversation_id). Verified: getChatRoomId=0, p_room_id/p_message_type=0, all 5 send_message calls use p_conversation_id. **DEPLOY ORDER: 198000+200000 land WITH/before 194000 is enforced, and `supabase test db` must pass first.** REMAINING (task #8): `app/messages/[jobId].tsx`:619 admin_support (room_id + phantom `chat_type` col → reconcile to ensure_help_support_conversation; admin-only so not breaking via nx_is_admin bypass), remove dead `src/core/chat/chatService.ts` (no importers), web in-mission team chat should adopt send_message (non-owner teammate metadata-trigger).

**P0 — mobile legacy chat breaks under our hardened `messages` RLS.** Mobile has TWO chat impls: (a) modern RLS-safe via `src/core/chat/hooks/useChat.ts`→`rpc('send_message')` + `src/hooks/useConversations.ts`→`ensure_job_conversation`/`ensure_help_support_conversation` (sets `conversation_id` server-side ✅); (b) LEGACY raw `from('messages').insert({job_id,sender_id,content})` with NO `conversation_id` in `app/messages/[id].tsx`(184/201), `app/messages/[jobId].tsx`(619), `app/chat/[job_id].tsx`, `src/core/chat/chatService.ts`(131). After we dropped `insert_chat_msgs` (194000) + allow-all read (192000), the ONLY messages policies (`msg_insert_party`, `msg_select_via_conv`, `view_chat_msgs`, `msg_team_*`) all key off `messages.conversation_id`→`conversations`. So a non-admin insert/select with `conversation_id IS NULL` is DENIED (admins bypass via `nx_is_admin()` → hides in testing). **→ legacy mobile chat send/read breaks the moment 192000/194000 are live.** Fix: route legacy screens through `ensure_job_conversation`+`send_message` (kill raw job_id inserts + `getChatRoomId` string-keying in `src/core/chat/messages.ts`); BACKFILL `conversation_id` for null rows or history vanishes; add pgTAP (null-conversation insert denied). **CORRECTS earlier wrong caveat: mobile does NOT use `job_messages`/`chat_rooms` (grep=0); those legacy tables are orphaned/open but unused → safe to lock/drop.**

**P1 STATUS — EXECUTED (Mobile Parity Phase 2, code+migrations written, PENDING db push + test).** Migrations: `20260801202000` (inspector_documents ADD reviewed_at+reviewed_by), `20260801204000` (work_orders_org_read permissive SELECT for org members → aligns useOperationsData). Code: consentService now imports the shared AUTHENTICATED `@/lib/supabase` (was a sessionless createClient(anonKey) → reads ran anon) + binds saveConsent/checkConsent to `auth.uid()` (ownerId). profiles drift fixed via PostgREST column ALIASES (keeps consuming code untouched): `years_experience:experience_years` in profile.tsx/(agency)jobs/explore/applicants.tsx/core/services/applications.ts + explore orderBy→`experience_years`; `is_featured:public_listing_featured` in explore. inspector_documents reads fixed via alias `user_id:inspector_id, document_type:doc_name, document_url:file_url` in verification/index.tsx + users/[id].tsx (+ filter fixed to inspector_id). Found 2 MORE drift sites than the audit listed (applicants.tsx, applications.ts). Verified: 0 remaining phantom years_experience/is_featured DB refs. **Also fixed a Phase-1 test bug:** rls_messages_silo_test.sql had inline `--` comments on `\set` lines (psql captures the whole line) → re-authored value-only; migrations themselves applied clean (backfill: 0 remaining null-conversation rows). ALIAS TECHNIQUE = the low-risk pattern for column drift (real_col aliased to the name code reads). Original P1 detail below:

**P1 mobile schema drift (PostgREST 400s, not silent):**
- `inspector_documents` cols are exactly `id,inspector_id,doc_name,file_url,expiry_date,status,created_at`. `app/(admin)/verification/index.tsx`(99/127) UPDATEs phantom `reviewed_at`/`reviewed_by` → approve FAILS; `app/(admin)/users/[id].tsx` types expect phantom `document_type`/`document_url`/`reviewed_*`. Fix: add review cols via migration OR use `status`; rename to `doc_name`/`file_url`.
- `profiles` has `experience_years`(int)+`years_of_experience`(text), NO `years_experience`, NO `is_featured`. Mobile selects `years_experience` (`app/(tabs)/profile.tsx`:188, `app/(agency)/jobs/[id].tsx`:1130) → whole query 400s; `app/(client)/explore/index.tsx` reads `is_featured`/`years_experience`. Fix → `experience_years` + `public_listing_featured` (same drift that broke web teaser push, commit 3327585).
- `consentService.ts` writes/reads `legal_consents` by a `userId` PARAM not `auth.uid()` → read breaks under owner-scope + insert forge vector. Fix: resolve auth.uid() internally, assert equality.
- `useOperationsData.ts`(43) reads `work_orders.eq('organization_id',…)` but RLS scopes to owner/client/inspector/user id → empty dashboards. Fix: add org-membership policy path or repoint to `jobs`/`nx_team_jobs`.

**P1 web:** baseline trigger L14260 `payout_status IN ('paid','released','complete')` — only `paid` valid (CHECK=unpaid/processing/paid/disputed); dead arms → hotfix to `='paid'`.

**P2:** `.single()`→`.maybeSingle()` (deleted-row/RLS-deny blanks); unfiltered realtime `messages` sub (filter by conversation_id); halalas↔cents naming footgun (numerically 1:1, not broken); chat signed-URL TTL mint-on-tap; dead web `?assigned_inspector_id=` filter param; add `legal_consents UNIQUE(user_id,document_id,policy_version)`.

**Verified CLEAN (ruled out):** web RPC/projection alignment 100% (the "156 called/42 defined" sweep was a grep artifact — baseline uses quoted `"public"."fn"`); price-blindness OK (qa:gr2); no service_role in client bundle; layouts use runWithRetry; React#31 fixed via dependency isolation (alias removed); Zod on server actions; offline outbox error-classification already shipped (#56). Evidence Vault `client_documents` cols EXIST (not dormant — see [[reference_nexpec_schema_gotchas]]).

Feeds the Mobile Parity Epic (Item 3) which also carries [[project_ghost_mode_team_chat]]. See [[project_rls_open_table_audit]], [[reference_master_feature_matrix]].

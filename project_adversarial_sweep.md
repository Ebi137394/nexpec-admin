---
name: project_adversarial_sweep
description: "Final 6-agent adversarial sweep (2026-06-26): auth/realtime/UI×2/parity/security. What got PATCHED vs the deferred P0s that must NOT be done blind."
metadata: 
  node_type: memory
  type: project
  originSessionId: 9ffed19b-c819-43de-a776-96b134eb176d
---

Final "zero-to-100" adversarial sweep — 6 read-only hunters (auth, realtime/chat, UI mobile, UI web, data parity, white-hat security), then triage+patch. All patches tsc-clean; outbox/price-blindness/rls-admin guards GREEN.

**PATCHED this sweep:**
- **Assignment-column drift (THE inspector-dashboard-empty bug):** `jobs.inspector_id` is a real column but NEVER written (dispatch writes `contractor_id`; `hired_inspector_id` only set post `inspector_start_job`). Fixed reads to `contractor_id`(+hired fallback) in `useInspectorData.ts` (powers (inspector)/dashboard — was permanently empty), `(inspector)/super-dashboard.tsx`, `core/hooks/useJobs.ts`, `(admin)/jobs/index.tsx`, `(admin)/live-radar.tsx`. Also rebuilt `useInspectorData` status map (it used non-existent urgent/active/pending). Web `PipelineSection.tsx` phantom status `'selected'`→`'CLIENT_SELECTED'`.
- **Price-blindness leaks:** `app/inspectors.tsx` (buyer surface, client_id filter) selected `payout_amount_cents` + showed inspector "Paid" → now `client_price_cents` only; added it to check-price-blindness BUYER_FILES (root-level file the scan missed). `apps/web/.../jobApplications.ts` dropped latent `bid_amount_cents`/`bidCents` from the buyer projection.
- **Migration `20260801244000` (PENDING push):** (1) dynamic sweep pinning `SET search_path=public,pg_temp` on EVERY owner=postgres SECURITY DEFINER fn in public missing it (closes the is_super_admin/is_service_role + money-family escalation primitive); (2) extended `guard_profile_privileged_columns` to block non-admin self-PATCH of is_verified/verification_status/verified_at/verified_by/balance_cents/stripe_connect_id/rating_*/completed_jobs_count (was role/id only → self-grant Verified bypass); (3) dropped `notification_preferences` USING(true) policy (owner policy already existed); (4) locked `reports` (was INSERT/SELECT USING(true) → owner-insert + owner/admin/project-client select).
- **Edge-fn IDORs/relay:** added caller authz to `send-consent-receipt` (owner/admin or webhook-secret), `generate-dispute-report` (dispute party/admin), `notify-job-assigned` + `notify-job-event` (admin OR `x-internal-secret`), `critical-alert-monitor` (fail-CLOSED when WEBHOOK_SECRET unset). DEPLOY NOTE: set env `NOTIFY_SHARED_SECRET` + ensure `WEBHOOK_SECRET` set + pg_net/DB-webhook callers send the secret headers, else those server-to-server paths 401.
- **Chat read path:** `app/chat/[job_id].tsx` SENT by conversation_id+attachment_url but READ by job_id+phantom file_url → new messages invisible. Fixed to resolve conversation_id at load + read/subscribe by it + attachment_url/_type columns (mirrors the now-modern `app/messages/[id].tsx`).
- **UI cleanup:** removed Persian/emoji debug console.logs (4 live screens), canGoBack guards on push-deep-link targets (job-details/[id], report/[id]), wired dead "View Draft" to existing report modal, surfaced silent errors in messages/[id], git-rm'd `app/trap_chat_screens.sh` + `messages/[id].tsx.DELETE_ME`; web NotificationBellLive console.log, inspectors pagination href="#"→span, notifications admin-only diagnostics link gated. `.single()`→`.maybeSingle()` on saved_jobs + applicants CLIENT_SELECTED.

**The 4 deferred P0s — NOW RESOLVED (2026-06-26 architectural surgery; all tsc-clean + guards GREEN; migrations PENDING push):**
1. **profiles bulk-PII leak → FIXED via migration `20260801248000`** (party-relationship policy, ZERO app-code change — the elegant/stable path). Dropped `profiles_authenticated_select_any USING(true)`; added `nx_can_read_profile(uuid)` SECDEF oracle + `profiles_read_related` policy USING it. A caller reads a profile only if: self / admin / shares-a-job (client/agency/contractor) / same-org (org_members) / application link (poster↔applicant). Self (profiles_read_self) + admin (profiles_read_admin) + the 160000 RESTRICTIVE deny all still compose. Every enumerated legit reader (admin screens, org/team, job-review counterparties, chat labels, reviews) falls in a branch → direct AND embedded reads keep working, all columns. Only a no-relationship competitor is blocked from bulk enumeration. RESIDUAL (by design): application branch lets a poster read own applicants' rows — bounded, UI-masked by jobApplications projection; drop later once applicants are RPC-only. SMOKE-TEST embedded profile reads of unrelated users (would blank, not crash).
2. **client_documents bucket → FIXED via migration `20260801246000`** (ZERO app-code). public=false + `client_documents_select_party_admin` storage policy covering 4 reader classes: storage-owner, admin, table-owner (client even if storage owner differs), assigned inspector (jobs.contractor_id) — mirrors the table RLS incl. its inspector_read. Existing createSignedUrl readers (admin vault, client vault/compliance) keep working.
3. **Mobile OAuth → MODERNIZED.** social-auth.ts now PKCE (`exchangeCodeForSession` on `?code=`, redirectTo=`Linking.createURL('oauth-callback')`); new `app/(auth)/oauth-callback.tsx` (cold-start deep-link exchange→route) + `app/(auth)/reset-password.tsx` (PASSWORD_RECOVERY→updateUser); sign-in resetPasswordForEmail got `redirectTo: createURL('reset-password')`; choose-role.tsx now calls `apply_onboarding_role` RPC (p_role; was the silently-dropped raw update) + `refreshOrganization` (was dead refreshProfile). EXTERNAL TODO (can't do in-repo): register the deep-link redirect URLs (`<scheme>://oauth-callback`, `<scheme>://reset-password`) in Supabase Auth + Google/Apple/LinkedIn consoles, then device-test.
4. **Inbox → CONSOLIDATED.** messages/index.tsx rebuilt on `useInbox()` (conversations backend; role-scoped; excludes job_team_internal); maps ConversationRow→row; tap routes jobId→/messages/[id] else /support-chat; old messages.job_id query + firehose channel removed. (Support 3-backend consolidation + orphaned-hook deletion + types/inspector.ts canonical-enum = remaining lower-pri cleanups, not P0.)

Hot-path verified: AuthContext loads `select('organization_id, role').eq('id', userId)` = self-read (profiles_read_self) → safe. New migrations 246000+248000 join 242000+244000 PENDING push (functions→web→mobile→db push order per runbook).

GHOST MODE + attachment signed-URL hardening + the 236000/242000 storage locks + money idempotency were re-verified CLEAN by the hunters.

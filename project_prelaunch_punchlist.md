---
name: project_prelaunch_punchlist
description: "Pre-launch agent-swarm findings (2026-06-25) — backlog now at ZERO after the 2026-06-26 zero-defect sprint (CHUNKS 1-4 + 2B); see top block + MASTER_RELEASE_RUNBOOK"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9ffed19b-c819-43de-a776-96b134eb176d
---

**ZERO-DEFECT SPRINT (2026-06-26) — backlog driven to ZERO; all tsc-clean + every QA guard GREEN (outbox/price-blindness/rls-admin). Master runbook at `docs/MASTER_RELEASE_RUNBOOK.md` (deploy order = functions→web→mobile→`supabase db push`, because storage lockdown breaks any client still on getPublicUrl).**
- **CHUNK 1 security+routing:** generate-contract direct call admin-gated + email echo removed + returns filePath (not getPublicUrl). ~21 routing fixes: `/jobs/[id]/*` cluster re-pointed to role groups; auth bounces `/login`+`/auth/login`→`/(auth)/sign-in`; canGoBack back-guards (messages/[id], contracts/[id]×3); `/ForgotPassword` wired to resetPasswordForEmail; dead `/transactions`→`/(inspector)/wallet/statement`; `/p/[id]`+`/profile/[id]`→`/(client)/inspector/[id]`.
- **CHUNK 2A storage dead-links:** every getPublicUrl on the 8 ALREADY-locked buckets → store-path + signedUrl() mint. Covered chat attachments (chat/[job_id] batch-mint), admin_attachment (admin/agency/tabs), report photos + final_report_doc (report/[id], submit-report, operations.ts, syncEngine.ts, review-report), contract PDF (contracts/[id] async-mint + FIXED 2 pre-existing bugs: `pdfUrl`→`uri` param mismatch + removed Google-gview privacy leak in contracts/view), resources (documents bucket), uploadReportImage. Render rule: mint in effect→state, never in JSX; lists batch-mint.
- **CHUNK 2B PII bucket lockdown — migration `20260801242000` (PENDING push):** locks receipts/inspector-docs/certifications/resumes/dispute-reports → private (public=false) + owner+admin SELECT; extends nx_can_access_doc with a dispute-reports PARTY branch (disputes.project_id→projects.client_id/inspector_id + raised_by). Upload→path + owner/admin read→mint across verification, admin verification/dashboard/users, cert-wallet, certifications, experience(resume), expenses/[id]+(inspector)/jobs/expenses, generate-dispute-report (server stores PATH, returns service-role createSignedUrl). **DECISION (admin-only-until-hire, golden-rule aligned):** raw CV/cert/receipt files NEVER shown to buyers/posters pre-hire — removed agency "View Inspector CV" + client "View Receipt"; (tabs)/jobs already gated CV pre-reveal. experience.tsx resume_url update got `// outbox-exempt:` pragma. avatars + company-logos stay public BY DESIGN (only remaining getPublicUrl).
- **CHUNK 3 brand cyan→#7C3AED:** 24 files (live+legacy src/screens), contrast-aware (cyan light-fill/dark-text → purple dark-fill/light-text). PRESERVED semantic palette: #3B82F6 info, status colors, file-type icons, accentInfo cyan-500, decorative neon/gradients. Only stray PRIMARY/accent cyan converted.
- **CHUNK 4 states:** my-jobs got loading/empty/error+retry + brand (#0F172A→#020420, #3B82F6 CTA→#7C3AED) + dev "Generate Test Job" __DEV__-gated; (client)/index surfaces load errors (was silent "No Projects") + removed Persian NAV-DEBUG logs; inspector-dashboard errors[] now RENDER (banner). discover/messages/index already had states. No applications select('*') over-fetch found. Web rfqs/opportunities colors already correct.
PRE-EXISTING (not this sprint, track later): check-db-refs flags withdrawal_requests + work_sessions (no CREATE migration); apps/web next.config ignoreBuildErrors still ON (~37 lucide/Suspense errors).

**HARDENING SPRINT STEP 2 (2026-06-26) — final transactional + ghost seals (tsc-clean, SQL self-tested, guard green; PENDING push):**
- **Contract-sign offline durability** → new outbox op `contract_sign` (outbox.ts OperationKind + operations.ts handleContractSign [idempotent on signer/state] + index.ts enqueueContractSign). `app/contracts/job/[id].tsx` handleSign now enqueues + honest queued-vs-signed feedback via opStillQueued (no false success, survives offline). Legacy v2 `app/contract/[id].tsx` RETIRED → `<Redirect href="/contracts" />` (was blind status='signed' false-success).
- **Wallet idempotency** → migration `20260801240000`: `wallet_restore_ledger(stripe_event_id PK)` + restore_wallet_balance now 3-arg `(uuid,bigint,text p_event_id)` idempotent (INSERT ledger ON CONFLICT DO NOTHING → skip credit on replay); **DROPPED 2-arg**; stripe-connect-webhook updated to pass `p_event_id: event.id` (DEPLOY WEBHOOK + PUSH TOGETHER). + partial UNIQUE index `uq_transactions_wallet_topup` backstops wallet_credit_topup (with dupe pre-check that fails loudly if prod has existing dup markers).
- **Ghost belt** → migration `20260801238000`: mark_conversation_read RETURNs early for admin + kind='job_team_internal' (no is_read flip, no unread reset → zero trace).
Step-2 deploy: ship client (contract_sign + v2 retire) + deploy stripe-connect-webhook, then push 238000/240000. All Red-Team P0s + P1s now sealed in code.

**HARDENING SPRINT STEP 1 (2026-06-26) — 3 critical DB P0s sealed (migrations PENDING push, ordered):**
- **#1 audit_events leak** → migration `20260801230000`: extended audit_redact_pricing to strip inspector IDENTITY (inspector_id/contractor_id/hired_inspector_id/assigned_inspector_id/applicant_id) + pricing; rebuilt audit_events_public as SECURITY DEFINER view (security_invoker=false) with internal row-scoping; DROPPED audit_events_select_parties + _scoped (raw table now admin-only). Client patched: clientJobReport.ts + disputesQueue.ts → audit_events_public. (PipelineSection reads non-existent event_kind/payload — separate pre-existing bug, left.)
- **#3 application self-hire** → migration `20260801234000`: removed rogue UPDATE jobs.status from validate_application_status_transition; added guard_application_self_transition trigger (applicant may only →'withdrawn'). withdrawApplication already fixed to 'withdrawn' (228000-era). Self-tests strip comments (no false-fail).
- **#2 document IDOR** → migration `20260801236000` (lockdown 7 buckets owner+admin + nx_can_access_doc oracle) + edge fn `supabase/functions/mint-doc-url` (service-role mint after authz) + src/core/storage/signedUrls.ts refactored to invoke mint-doc-url. **REROUTE DONE (2026-06-26):** all 12 chat/contract direct createSignedUrl callers switched to signedUrl() (messages/[id] ×3, support-chat ×3, (admin)/support-chat/[user_id] ×3, useConversations signPath, core/chat/messages getChatAttachmentSignedUrl, contracts/index) + signedUrls.ts invoke calls outbox-exempt-pragma'd; tsc 0, guard green. Uploader self-mint (core/chat/messages.ts uploadChatAttachment) left as-is (owner can mint post-lockdown). Step 1 push-ready. Deploy order: deploy mint-doc-url fn + ship client FIRST, then `supabase db push`. **Historical ⚠️:** the lockdown BREAKS ~14 direct `createSignedUrl` callers (chat_attachments: app/messages/[id].tsx, app/support-chat.tsx, app/(admin)/support-chat/[user_id].tsx, src/hooks/useConversations.ts:80, src/core/chat/messages.ts:386/433; contracts/index.tsx:886; + secondary helpers packages/shared-core/src/storage/signedUrls.ts + src/core/supabase/supabase.ts createSignedUrl/createSignedUrls). MUST reroute these to signedUrl() AND deploy mint-doc-url BEFORE pushing 236000, else chat/contract viewing breaks. Buckets read via signedUrl() helper (report-images/documents/job-documents/inspection-photos×2/inspection-reports) are auto-covered. nx_can_access_doc linkage covers inspection_reports/contracts/project_documents/jobs.template_url/messages — a missed doc type fails CLOSED (over-strict, not a hole). Deploy order: (1) reroute callers, (2) deploy edge fn + client, (3) push 230000/234000/236000.

**ROUND 3 — RED TEAM / CHAOS (2026-06-26). VERDICT: NO-GO until the open P0s below are sealed.** 5 adversarial agents (concurrency, payments/webhooks, ghost/audit, offline, auth/doc-access). FIXED this round (tsc/guard green; migrations 226000 role-guard + 228000 accept_offer-revoke PENDING push): self-admin role escalation (versioned BEFORE UPDATE trigger guard_profile_privileged_columns), accept_offer self-assign broker-bypass (REVOKE anon/auth; 0 callers), withdrawApplication wrote 'rejected'→now 'withdrawn', web fetchAdminConversations now excludes job_team_internal.
OPEN P0 (MUST fix before launch): (1) **audit_events RAW-table leak** — audit_events_select_parties/_scoped let inspector(contractor_id)/client read un-redacted inspector_payout_cents + platform_spread_cents AND inspector identity (contractor_id/hired_inspector_id) PRE-reveal; redaction lives only in audit_events_public view/TS. Fix=REVOKE SELECT on audit_events from authenticated + drop party policies + force non-admin through a SECURITY DEFINER redacting view; add identity to redactor. (2) **Cross-tenant document IDOR** — 7 storage buckets (report-images, documents, job-documents, chat_attachments, inspection-photos, inspection-reports, contracts) have SELECT policy `USING(bucket_id='X')` = any authenticated; mobile mints signed URLs client-side from caller-supplied path → user B reads user A's docs/contracts/chat if path known. Fix=path/party-scoped storage.objects policies (mirror cdocs_storage_*) + server mint RPC. (3) **restore_wallet_balance non-idempotent** — connect-webhook payout.failed retry double-credits wallet (220000 only revoked anon/auth, body still bare additive). Fix=idempotency ledger keyed on stripe payout/event id (needs signature+webhook change). (4) **validate_application_status_transition trigger** — inspector sets applications.status='accepted' (RLS allows any value) → trigger forces jobs.status='in_progress' w/ contractor_id NULL (self-hire). Fix=drop jobs side-effect + value-constrain inspector RLS. (5) contract sign (app/contracts/job/[id].tsx) is direct online RPC not outbox → lost offline; app/contract/[id].tsx blind status='signed' false-success on 0-row. OPEN P1: report→photo orphan (link by client_op_id not id), wallet_credit_topup no unique backstop, mark_conversation_read admin branch ghost-trace (no kind guard), legacy mobile admin-inbox.tsx + communications/index.tsx read all messages (team_internal leak — retire/migrate), fetch_cert_by_slug returns supplier name to anon. LIVE-DB VERIFY (baseline may be stale): stripe_complete_job RPC existence, jobs_status_check has review_pending?, audit_events has event_kind/payload?, profiles role-guard trigger exists? CONFIRMED SOLID: admin_dispatch_job (super_admin-gated+FOR UPDATE+status precondition → double-dispatch SAFE), money-RPC idempotency (FOR UPDATE+client_op_id unique), webhook signature-verify + claim-then-process, ghost CORE (RESTRICTIVE admin-post block, zero-trace admin_open_internal_thread), escrow contract-before-money, session-expiry outbox handling, owner-scoped buckets (client_documents/inspector-docs/etc).

**DB PUSH GOTCHAS (2026-06-26, both fixed):** (1) a `pg_get_functiondef` price-guard `ILIKE '%budget%'` etc. FALSE-TRIPPED on the function's OWN in-body comment that named the excluded columns → wrap with `regexp_replace(def,'--.*','','g')` to strip SQL comments before scanning. (2) `inspector_earnings` owner col is **`user_id`** (NOT inspector_id) — a 222000 policy guessed inspector_id → would 42703. Verified owner cols: inspector_earnings=user_id, inspector_documents=inspector_id, certifications=user_id, signed_agreements=user_id, documents=organization_id (+org_members(org_id,user_id)), wallets=user_id. Always read the CREATE TABLE before writing an RLS policy column.

**ROUND 2 — 12-agent domain swarm (2026-06-26).** FIXED this round (tsc clean, guard green 227, 2 new migrations PENDING db push): `restore_wallet_balance` money-mint RPC (migration 20260801220000 — internal admin/service guard + REVOKE anon/authenticated); anon-exposure on 10 RLS-off tables (migration 20260801222000 — REVOKE anon/PUBLIC on all 10 + full lock [ENABLE RLS + revoke authenticated writes + admin/owner SELECT] on platform_wallet/payment_audit_log/inspector_earnings/push_token_history/chat_rooms/job_messages); lifecycle P0s report/[id].tsx + (tabs)/jobs/[id].tsx (removed inspector/buyer direct jobs.status='completed' writes); (client)/create.tsx status open→pending_approval; job-details/[id].tsx [id] param bug (notification deep-link); AuthContext push-token delete on signOut + fetchOrganization retry (false-logout); useConversations admin inbox now excludes job_team_internal (ghost mode).
OPEN P0/P1 from round 2: (a) **RLS owner-table FOLLOW-UP** — inspector_documents/certifications/signed_agreements/documents still have authenticated cross-tenant access (anon closed); need ENABLE RLS + owner policies (verify owner cols). (b) Ghost-mode DB belt: mark_conversation_read admin branch should skip job_team_internal (P2 root-cause). (c) Storage: ~15 getPublicUrl on private buckets (enumerated by agent) → signedUrl. (d) Offline: outbox photo orphaned on submit-report (link by client_op_id not id); check-outbox-routing.mjs WRITE_RPCS allowlist too narrow. (e) UI: src/constants/theme.ts primary=#00F5FF cyan (should be #7C3AED) is root cause of cyan leaks; ~6 screens off-brand bg/buttons. (f) Routing: redirect stubs drop [id]; back() dead-ends on notification cold-start. (g) process_withdrawal missing SET search_path. Full detail below.

---

A 6-agent swarm audited mobile pre-launch (nav, broker-flow, price-blindness, parity, uploads, silent-failures). Pattern across all six: **canonical route groups are hardened; legacy flat-folder / duplicate screens carry the bugs.**

**FIXED this session (Wave 9 + swarm cleanup, all tsc-clean, guard GREEN):**
- All ~12 blob-upload sites → base64/decode (see [[reference_upload_blob_antipattern]]).
- `(inspector)/jobs/[id]/submit-report.tsx` P0 param bug: read `jobId` but route segment is `[id]` → reports were enqueued with `job_id=undefined`. Now `const { id: jobId } = useLocalSearchParams`.
- False-success writes (unchecked `error` then "Success" alert): `(shared)/agency-job-details.tsx` + root `agency-job-details.tsx` (applications/jobs update) + `(admin)/jobs/[id].tsx publishReport` → now `if (error) throw`.
- `my-jobs.tsx generateTestJob` cheat (inserted job with `contractor_id=self`) → `__DEV__`-gated.

**P0 — ALL FIXED (Final Lockdown 2026-06-25):**
1. **`discover_jobs` RPC budget leak** → migration `20260801218000_discover_jobs_price_blind.sql`: replaced `to_jsonb(c) - 'dist_km'` with an explicit jsonb_build_object allowlist (INSPECTOR_JOB_FIELDS + scope_template_id + payout cols; ZERO budget/price/spread). CREATE OR REPLACE keeps signature+grants. Includes a pg_get_functiondef guard DO-block that ROLLS BACK the migration if any budget/client_price/price_cents/platform_spread/margin reappears. **PENDING `supabase db push`.**
2. **`app/map.tsx` budget→inspector** → role-branched the SELECT (inspector projection omits `budget`) + null-coerced budget in the marker mapping (pill gated on budget!==null → hidden; sheet shows 'Budget TBD'). tsc clean.
3. **`app/find-jobs.tsx` broker bypass** → RETIRED to a `<Redirect href="/(tabs)/jobs" />` stub; all self-claim / reports-insert / jobs.status-write logic deleted. Outbox baseline 231→229 (its 2 writes gone).

**OPEN — P1:**
- **getPublicUrl on PRIVATE buckets** (~15 sites: chat/[job_id], report/[id], verification, certifications, cert-wallet, experience, branding-settings, resources, expenses×2, contracts/[id], contract/[id], admin/support-chat, core/offline/operations). Only `avatars` is public post-lockdown → these URLs 400. Fix = store path + mint signedUrl (src/core/storage/signedUrls) on read. Dedicated wave; verify each bucket's privacy first.
- **Route collisions / duplicates**: `agency-job-details.tsx` (root) vs `(shared)/agency-job-details.tsx` read different param keys (`id` vs `jobId`) → non-deterministic; bare `/jobs/[id]` folder collides 4-way with the grouped versions; several bare `/jobs/${id}/contract|expenses|apply` pushes drop their route group. Decide canonical, retire bare/dupe.
- **`/auth/login` & `/login`** nav targets don't exist (profile/rates.tsx:455 live; messages/[jobId].tsx + app/submit-report.tsx legacy) → should be the real auth route (confirm name; (auth) group exists per baseline but glob-parens hid it).
- **`src/core/services/contracts.ts hireContractor`**: unchecked status updates + `finalPrice = ... || 100` invents $100. Likely dormant; verify callers.
- **Mobile admin portal parity gaps** (web-only): admin RFQ markup/marketplace, withdrawal reject-with-reason, Tax Center admin console, dispatch matching engine (shortlist/auto-match/preview), supplier-contract generate/countersign, admin commercial-revision decide/counter, admin invoice list + reassign_invoice_department, admin internal-thread monitor. These are real epics, not quick fixes.

**Confirmed CLEAN (don't re-audit):** modern (client)/(agency)/(inspector) route groups uphold broker invariants + contractor_id canonical; buyer nominations only set applications.status='CLIENT_SELECTED'; reject flows single-update no cascade; central lib/jobsProjection.ts (BUYER/INSPECTOR_JOB_FIELDS) keeps most surfaces price-blind; flash-reports/disputes/contracts e-sign/bidding/budgets at parity; Stripe named-disclosure web-only is BY DESIGN.

**NEEDS HUMAN CHECK (product calls):** does client see inspector bid pre-dispatch ((client)/jobs/[id]/applicants.tsx renders bid_amount — strict GR2 says no); does mobile CCI-applications flow supersede web's admin_review_credential.
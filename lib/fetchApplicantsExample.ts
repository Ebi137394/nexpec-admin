// ════════════════════════════════════════════════════════════════════════════
//  lib/fetchApplicantsExample.ts — LANE-A-PHASE-1 example-file retirement
//
//  Pre-strike: a "CORRECTED VERSION" reference template for fetching job
//  applicants with Supabase. Zero production imports — the canonical
//  fetchApplicants logic lives inline in:
//      - app/(agency)/jobs/[id].tsx
//      - app/client/jobs/[id]/applicants.tsx
//      - app/(admin)/* moderation flows
//
//  Verification (LANE-A audit + Phase-1 grep):
//      $ grep -r "fetchApplicantsExample" — zero non-self matches.
//
//  Post-strike: empty module preserved as a stub. Any future import
//  surfaces a deprecation error via TypeScript (no exported symbols).
// ════════════════════════════════════════════════════════════════════════════

/** @deprecated LANE-A-PHASE-1 — example template, never imported. */
export const __DEPRECATED_LANE_A_PHASE_1 = true;

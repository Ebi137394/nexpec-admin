diff --git a/node_modules/react-native-nitro-modules/android/src/main/java/com/margelo/nitro/NitroModulesPackage.kt b/node_modules/react-native-nitro-modules/android/src/main/java/com/margelo/nitro/NitroModulesPackage.kt
index f55986b..b47aefd 100644
--- a/node_modules/react-native-nitro-modules/android/src/main/java/com/margelo/nitro/NitroModulesPackage.kt
+++ b/node_modules/react-native-nitro-modules/android/src/main/java/com/margelo/nitro/NitroModulesPackage.kt
@@ -23,13 +23,20 @@ class NitroModulesPackage : BaseReactPackage() {
       val moduleInfos: MutableMap<String, ReactModuleInfo> = HashMap()
       val isTurboModule: Boolean = BuildConfig.IS_NEW_ARCHITECTURE_ENABLED
       moduleInfos[NitroModules.NAME] =
+        // NEXPEC: positional args, not named. React Native 0.76's
+        // ReactModuleInfo primary constructor declares its first four
+        // parameters as private `_name`, `_className`,
+        // `_canOverrideExistingModule`, `_needsEagerInit`, so the named form
+        // upstream uses (which matches RN 0.83) does not resolve here. The
+        // parameter ORDER is identical on both, so positional compiles
+        // against 0.76 and 0.83 alike.
         ReactModuleInfo(
           NitroModules.NAME,
           NitroModules.NAME,
-          canOverrideExistingModule = false,
-          needsEagerInit = false,
-          isCxxModule = false,
-          isTurboModule = isTurboModule,
+          false, // canOverrideExistingModule
+          false, // needsEagerInit
+          false, // isCxxModule
+          isTurboModule,
         )
       moduleInfos
     }

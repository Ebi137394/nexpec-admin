---
name: reference-web-build-typecheck-gated
description: apps/web build gates BOTH ENABLED — types (c070366) + ESLint flat config (0e40786); ignoreBuildErrors AND ignoreDuringBuilds both removed
metadata: 
  node_type: memory
  type: reference
  originSessionId: 49b95114-7fa8-48bb-91e6-6eeb9c30c3df
---

`apps/web/next.config.mjs` has **`typescript.ignoreBuildErrors: true` + `eslint.ignoreDuringBuilds: true`** (added 2026-05-30, ebi approved, to ship the 1.0 cinematic landing). They were needed because `next build` (run by Vercel via `vercel.json` → `cd ../.. && yarn build:web` → `next build`) otherwise fails its "checking validity of types" phase on **~37 PRE-EXISTING type errors** — a lucide-react icon-typing regression (`ForwardRefExoticComponent` not assignable to `ComponentType<{className?,strokeWidth?}>`) across admin pages (PipelineSection, admin/reviews, StructureDialogs, VaultDocumentActions, OnboardingWizard, compliance/templates, admin/Header, specialties-bulk…) plus a `Suspense` JSX-types issue in `verify/page.tsx`. None are in the marketing/landing code.

**RESOLVED 2026-05-31 (commit c070366):** all 36 errors cleared and **`typescript.ignoreBuildErrors` REMOVED** — `next build` re-enforces types again. Fixes: (1) lucide icon props typed as `LucideIcon` (Sidebar.tsx's blessed pattern) across PipelineSection/admin-reviews/VaultDocumentActions/compliance-templates/specialties-bulk; (2) the ReactNode + Suspense errors were the SAME dual-@types/react split as the runtime #31 (root @types/react 18 vs web 19 cross-contaminating the global React/JSX namespace) — deduped via `apps/web/tsconfig.json` `paths` (`react`/`react-dom` → `./node_modules/@types/react(-dom)`); runtime React still resolves to the real package via the next.config webpack alias (types vs bundling are separate phases); (3) two supabase `GenericStringError` casts routed through `unknown`. GOTCHA learned the hard way: do NOT point the tsconfig `react` path at `./node_modules/react` (the real pkg has no .d.ts → TS7016 `any` cascade, 146 errors) and do NOT add `react/*` wildcards (breaks subpath types) — point bare `react`/`react-dom` at their `@types`. **ESLINT NOW CONFIGURED + ENFORCED (2026-05-31, commit 0e40786).** Discovery: ESLint was NEVER set up (no config, no deps) — the `ignoreDuringBuilds` flag masked its absence, not errors. Added `apps/web/eslint.config.mjs` (ESLint 9 flat config, extends `next/core-web-vitals` via FlatCompat). PRAGMATIC BUG-ONLY severities: crash-class rules ERROR (react-hooks/rules-of-hooks, react/jsx-key, core "possible problems"); stylistic noise OFF (no-unescaped-entities, no-img-element, no-html-link-for-pages, display-name, no-unknown-property); behavior-risky/dead-code WARN (exhaustive-deps, jsx-no-target-blank, @typescript-eslint/no-unused-vars w/ `^_`). `next lint` is DEPRECATED (gone in Next 16) → `lint` script is now `eslint .`. devDeps: `eslint@^9`, `eslint-config-next@15.5.18`, `@eslint/eslintrc@^3.1.0`. `next.config.mjs` eslint = `{ dirs: ['src'] }` (no ignoreDuringBuilds). CI `web · lint` + `web · typecheck` BOTH blocking now. REQUIRES `npm install` to write the toolchain into package-lock.json before build/CI lint runs (sandbox couldn't install — npm registry network-blocked, so config was authored unverified; user runs `npm run lint -w @nexpec/web` to confirm clean). Authoritative gates: `npm run typecheck -w @nexpec/web` + `npm run lint -w @nexpec/web`. See [[reference-web-react-dual-version]], [[reference-ots-and-release]].

**UPDATE 2026-06-04 — ESLint config is DEGRADED on the deployed branch (Vercel repo `github.com/Ebi137394/nexpec-admin`, and matches local `/Users/ebrahimfeyzi/Desktop/nexpec`).** `next build`/`next lint` both print "⚠ The Next.js plugin was not detected in your ESLint configuration" — meaning neither the Next plugin NOR `@typescript-eslint` is actually loaded. Consequence (bit us, build 42d7474): any `/* eslint-disable @typescript-eslint/* */` directive → **hard error "Definition for rule not found" → fails `next build`**; `@next/next/*` disable directives are inert ("unused directive" warnings). RULE OF THUMB until the flat config is fixed: do NOT write `eslint-disable` comments referencing `@typescript-eslint/*` or `@next/next/*` in apps/web — just use `any`/`<img>` directly (the rules aren't active so they won't flag). Fixed visionModel.ts (removed the no-explicit-any disable) + ai-coinspector/page.tsx (removed 3 inert directives); `npx next lint` → "✔ No ESLint warnings or errors", `tsc` 0. TODO (ebi): repair `apps/web/eslint.config.mjs` so the Next plugin actually registers.

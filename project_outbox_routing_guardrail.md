---
name: project-outbox-routing-guardrail
description: Capture + flash-report flows rewired through the outbox + CI guardrail enforcing it; lib/offline shim gotcha; remaining field-write debt sweep
metadata: 
  node_type: memory
  type: project
  originSessionId: 49b95114-7fa8-48bb-91e6-6eeb9c30c3df
---

Closed the QA "Failure 2" data-loss hole: `app/(inspector)/compliance/job/[id]/capture.tsx` used to write `inspection_captures` (photo/GPS/text) and `pi_record_ai_detection` DIRECTLY, so offline captures failed silently. Now everything routes through the offline outbox. See [[reference_offline_sync_hardening]] and [[project_provable_ai_loop]].

What shipped (all in `/Users/ebrahimfeyzi/Desktop/nexpec`):
- Two new `OperationKind`s — `capture_save` + `ai_detection` — in `src/core/offline/outbox.ts`; handlers `handleCaptureSave` (deferred file upload via upsert→idempotent, then insert; idempotent on the client PK `id`) + `handleAiDetection` (`pi_record_ai_detection` with `p_client_op_id`) in `operations.ts`; `enqueueCaptureSave`/`enqueueAiDetection` in `index.ts`.
- **Chain-integrity subtlety:** `prev_capture_sha256` now derives from LOCAL `captures` state (`captures[captures.length-1].capture_sha256`, array loads oldest→newest), NOT `fetchPrevCaptureHash()` which hits the network and returns null offline → would silently break the per-job hash chain. `sort_index` was already locally-derived; this matches it.
- Migration `supabase/migrations/20260717120000_ai_detection_idempotency.sql`: adds `ai_detections.client_op_id` + partial unique index; DROP+CREATE `pi_record_ai_detection` (sig changed → re-GRANT EXECUTE to authenticated) with trailing `p_client_op_id text DEFAULT NULL`, idempotency short-circuit BEFORE the binding checks (a replay must succeed even if the model was later unpublished), + unique_violation→return existing id. The 20260715 provable-AI binding is reproduced VERBATIM.

**GOTCHA — dual offline module:** `lib/offline/*` are transparent re-export shims (`export * from '@/src/core/offline/*'`). `@/*`→`./*`, so `@/lib/offline` resolves through the shim to the canonical `src/core/offline`. EDIT `src/core/offline`, not `lib/offline`. New code imports from `@/lib/offline`.

**FLASH-REPORT (NCR) raise — same bug class, 2nd round (user-reported via screenshots, "TypeError: Network request failed", OBX 0·0·0·0):** `app/jobs/[id]/flash-reports/new.tsx` → service `src/lib/flashReports.ts` did 3 direct writes (`flash_report_create` RPC → `storage.upload` → `flash_report_add_attachment` RPC). My FIRST guardrail MISSED it twice over: wrong dir (`app/jobs`, not `app/(inspector)`) AND service-layer indirection (mutation in `src/lib`, not the screen). Fix: ONE composite `flash_report_raise` outbox op (kind in outbox.ts, `handleFlashReportRaise` in operations.ts, `enqueueFlashReportRaise`+`newClientId`+`opStillQueued`+`flushQueue` exports in index.ts). Composite (not 3 ops) because attachments reference the report id and `flash_report_add_attachment` raises P0002→404→**fatal** in the classifier — a separate attachment op draining before create would be abandoned. `raiseFlashReport(input, attachments)` mints a client id, derives storage paths from the LOCAL session (`getSession()` works offline), enqueues, and — online — awaits `flushQueue()` then checks `opStillQueued` to decide navigate-to-report vs. "saved, will sync" + list. Migration `20260718120000_flash_report_offline_idempotency.sql`: `flash_report_create` gains `p_client_id` (DROP+CREATE+re-GRANT, idempotent replay) + `COALESCE(p_client_id, gen_random_uuid())`; `flash_report_add_attachment` dedups on a new unique index `(flash_report_id, storage_path)` via ON CONFLICT. Bodies reproduced VERBATIM otherwise.

CI guardrail (the enforcement), now HARDENED after it missed the flash bug: `scripts/qa/check-outbox-routing.mjs` (zero-dep Node) fails the build on any direct supabase write inside `SCAN_DIRS = ['app','src']` (was just `app/(inspector)` — too narrow), excluding `src/core/offline` + `src/core/supabase`. Patterns: `.insert/.update/.upsert/.delete`, **`.upload(`/`createSignedUploadUrl` (storage), `.functions.invoke(`**, and write-RPCs in `WRITE_RPCS` (now incl. the 3 flash RPCs). Per-file `supabase`-import filter kills Map/Set `.delete` false positives. Baseline `scripts/qa/outbox-routing-allowlist.json` grandfathers debt as a **multiset** (243 entries now), line-number-free. Escape hatches: inline `// outbox-exempt: <reason>` (used on capture.tsx `generate-vca` — an interactive ONLINE affidavit gen, legitimately not queueable) or re-baseline. `npm run qa:outbox` + BLOCKING CI step. Proven: catches storage.upload + write-RPC + functions.invoke; green clean.

The hardened guard immediately caught a residual `supabase.functions.invoke('generate-vca')` bypass in capture.tsx that round 1 missed (functions.invoke wasn't a pattern then) — but generate-vca is genuinely online-only (validates all captures synced server-side, returns a verify URL), so it's pragma-exempted, not queued.

**flash_report_transition — now routed (3rd round).** `flashReports.ts:transitionFlashReport` enqueues a `flash_report_transition` op instead of a direct RPC. Handler `handleFlashReportTransition` does pre-check idempotency (read status; if already == toStatus, no-op — a replay of an applied transition would otherwise hit "illegal X→X" → 22000→400→fatal→wrongly abandoned). Illegal/now-invalid transitions stay fatal (surfaced). NO migration (the flash_reports table + RPCs live in repo-ROOT `20260512160000_flash_reports.sql`, NOT supabase/migrations/ — unversioned-location gotcha). `enqueueFlashReportTransition` does NOT auto-flush; `transitionFlashReport` awaits flushQueue when online so the detail screen's reload sees the applied state.

**Realtime resilience primitive (final sweep).** New `src/core/realtime/useRealtimeSubscription.ts` — status-aware Supabase subscription: `onDesync` (refetch) on CHANNEL_ERROR/TIMED_OUT/CLOSED, clean removeChannel teardown, instance-unique channel name (pass a `useId()` suffix). Fixes the rampant "subscribe with no status callback → feed silently dies after a network drop, never reconnects" pattern. APPLIED everywhere: `useCriticalAlerts` + `useWallet` first, then 23 MORE feeds via 4 parallel agents (messaging ×6, contracts+admin ×6, shared hooks ×6, role hooks ×5 — chat/radar/inbox/contracts/dashboards/earnings/jobs). 25 feeds total → silent-death class CLOSED. All tsc-clean (the 22 remaining errors are pre-existing Supabase `never`-typing in fetch code, confirmed via git-diff hunks NOT overlapping error lines). Multi-table hooks fold listeners into one subscription with N `bindings`, routing on `payload.table`/`payload.eventType`. Also fixed a real leak: `app/messages/[jobId].tsx` `subscribeToRealtimeMessages` reassigned channelRef without removeChannel → duplicate INSERT handlers (now tears down first).

Remaining field-write debt (≈239 grandfathered across app+src — burn-down list): `expenses.tsx`, `wallet/withdraw.tsx` (`process_withdrawal` — see [[project_financial_security_crisis]]), `cert-wallet.tsx`, `profile/verification.tsx`, etc. New bypasses fail the build; fixed files (capture.tsx, flashReports.ts raise+transition, new.tsx, AddFundsModal) are protected.

PENDING: `supabase db push` BOTH `20260717` (ai_detection) + `20260718` (flash_report) migrations before those paths are live in prod (the outbox retries until then). All changed .ts/.tsx files tsc-clean (only pre-existing errors remain in `src/core/supabase/supabase.ts` + `src/features/compliance/lib/capture.ts`).

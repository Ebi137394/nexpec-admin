{
  "_comment_canonical_config_lives_in": "app.config.js — that file overrides every value in this stub. This file remains only because some Expo CLI tooling still reads app.json for slug detection before evaluating app.config.js.",
  "_comment_phase_5_hour_4": "Hardening pass NX-IDENT-001 collapsed the previous duplicated config block (which contained a stale `com.yourname.nexpec` bundle id and a full set of permission strings that conflicted with app.config.js) down to this minimal stub.",
  "expo": {
    "name": "NEXPEC",
    "slug": "nexpec"
  }
}
